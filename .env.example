# Application Environment Configuration
PORT=3000
NODE_ENV=development

# MongoDB Connection String (optional; in-memory store active when omitted)
MONGO_URI=

# JWT Secret for cookie authentication
JWT_SECRET=fixit_jwt_secret_super_key_2026

# Gemini API Key (server-side)
GEMINI_API_KEY=
